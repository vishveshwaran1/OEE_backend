const PartDetails = require('./partdetails');
const HourlyProduction = require('./hourlyProduction');
const Rejection = require('./rejection');
const OEE = require('./oee');
const StopTime = require('./stoptime');
const Correction = require('./correction');
const PlanActual = require('./planActual');

module.exports = {
    PartDetails,
    HourlyProduction,
    Rejection,
    OEE,
    StopTime,
    Correction,
    PlanActual
};